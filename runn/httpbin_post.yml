desc: Generated by `runn new`

debug: true

runners: # runbook 内で実行する各種 Runner を定義
  req: http://httpbin.org

vars:
  asa_gohan: ${RUNN_ASA_GOHAN}

steps:
- desc: "httpbin の POST を叩いてみます"
  req:
    /post:
      post:
        body:
          application/json: 
            asa_gohan: "{{vars.asa_gohan}}"
            oyatsu: "karl"

- desc: "httpbin の POST 2 回目を叩いてみます"
  req:
    /post:
      post:
        body:
          application/json: 
            asa_gohan: "{{vars.asa_gohan}}"
            oyatsu: "karl"
